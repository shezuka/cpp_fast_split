#ifndef FAST_SPLIT_EXPLODE_H
#define FAST_SPLIT_EXPLODE_H

#include "exploder.h"

exploder explode(const char *source, const char *delim, const size_t limit = 0);

#endif //FAST_SPLIT_EXPLODE_H
